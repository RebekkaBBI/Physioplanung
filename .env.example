# Optional: Wenn gesetzt, läuft die App im Cloud-Modus (Supabase Auth + Postgres).
# Projekt in der EU-Region anlegen (Supabase Dashboard → Project Settings → Region).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
